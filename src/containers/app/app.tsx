import React from 'react';
import UserRouter from '../../routers/user';

const AppViewWeb = () => {
    return (
        <div>
            <UserRouter />
        </div>
    );
};

export default AppViewWeb;